{
  "name": "exit-velo",
  "version": "2.0.0",
  "type": "module",
  "description": "Exit Velo — baseball analytics publication",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "optimize": "node scripts/optimize-images.js",
    "publish": "node scripts/optimize-images.js && fly deploy",
    "lint": "eslint . && stylelint \"src/css/**/*.css\"",
    "lint:js": "eslint .",
    "lint:css": "stylelint \"src/css/**/*.css\""
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.0.0",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
